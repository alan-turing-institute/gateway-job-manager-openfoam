version: '3'
services:
  manager:
    build: .
    ports:
     - "5001:5001"
     - "10023:22"
    volumes:
     - .:/app
    env_file:
      - ./keys/simulator.env
    environment:
      - FLASK_CONFIGURATION=development
      - ENV_FLASK_APP=app.py
